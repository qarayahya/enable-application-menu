# Enable Application Menu

This plugin is an experimental concept that replaces the default block editor logo button with an application-style menu. It’s not intended for production use, but rather as a small UI experiment to explore how an app-like menu could work inside the WordPress block editor.
